# 📝 Todo List App

Удобное и современное приложение для управления задачами. Собрано на Flutter с локальной синхронизацией через Realm. Быстрое, аккуратное и кроссплатформенное — работает на iOS и Android.

Краткое описание:
Manage your tasks effortlessly with our todolist, built on Realm. Sync tasks instantly, switch between light and dark modes depending on your mood, and never lose sight of your goals!

---

## ✨ Ключевые возможности
- ⚡ Высокая производительность — оптимизированная работа с локальной базой.
- 🔄 Мгновенная синхронизация — данные синхронизируются через Realm.
- 🌓 Поддержка тёмной и светлой темы — хранение предпочтений пользователя.
- 💾 Надёжное локальное хранилище задач.
- 🎨 Современный интерфейс, ориентированный на продуктивность.
- 📱 Кроссплатформенность: одна кодовая база для iOS и Android.

---

## 🛠 Технологический стек и состав репозитория
Основные технологии:
- Flutter (Dart)
- Realm (локальная БД и синхронизация)
- Архитектура управления состоянием: cubit / bloc (в репозитории присутствуют соответствующие директории)
- Material Design 3

Языковая композиция репозитория (по содержимому):
- C++ — 32.8%
- Dart — 32.5%
- CMake — 27.3%
- HTML — 2.6%
- Swift — 2.5%
- C — 2.0%
- Other — 0.3%


---

## 🚀 Быстрый старт

### Требования
- Flutter SDK 3.0+
- Git
- (При необходимости) инструменты для сборки нативных модулей (Android NDK/CMake, Xcode)

Проверить версию Flutter:
```bash
flutter --version
```

### Установка и запуск
1. Клонируйте репозиторий:
```bash
git clone https://github.com/Beksanoff/todo-list.git
cd todo-list
```

2. Установите зависимости:
```bash
flutter pub get
```

3. Запустите приложение:
```bash
flutter run
```
или на конкретном устройстве:
```bash
flutter run -d <device_id>
```

4. Сборка релизной версии

iOS:
```bash
flutter build ios --release
```

Android (APK):
```bash
flutter build apk --release
```

Google Play (AAB):
```bash
flutter build appbundle --release
```

---

## 📁 Структура проекта (важные файлы и директории)
- lib/
  - app.dart — конфигурация приложения (настройка тем, провайдеров и т.д.)
  - main.dart — точка входа приложения
  - cubit/
    - theme/ — логика управления темами (Cubit / Bloc для переключения светлой/тёмной темы)
  - features/ — фичи приложения (UI и бизнес-логика по функциональным модулям)
  - repository/ — слой доступа к данным (Realm, локальное хранилище)
  - ui/ — виджеты и экраны пользовательского интерфейса
- test/ — тесты
- pubspec.yaml — зависимости и конфигурация
- README.md — этот файл
- LICENSE — лицензия проекта (проверьте наличие и содержание)

---

## 🗄 Схема модели Task (пример)
Модель Task содержит типичные поля:
- id: String — уникальный идентификатор
- title: String — название задачи
- description: String? — описание (опционально)
- isCompleted: bool — состояние выполнения
- createdAt: DateTime — дата создания
- dueDate: DateTime? — срок выполнения
- priority: String — приоритет: low | medium | high
- completedAt: DateTime? — дата завершения (опционально)

  
---

## 🧪 Тестирование
- Запуск юнит-тестов:
```bash
flutter test
```
- Покрытие:
```bash
flutter test --coverage
lcov -l coverage/lcov.info
```
- Интеграционные тесты:
```bash
flutter test integration_test
```

---

## 🤝 Как внести вклад
1. Fork репозитория и создайте ветку:
```bash
git checkout -b feature/your-feature
```
2. Внесите изменения, добавьте тесты и убедитесь, что всё работает.
3. Запустите тесты:
```bash
flutter test
```
4. Коммит и push:
```bash
git add .
git commit -m "docs: improve README"
git push -u origin feature/your-feature
```
5. Откройте Pull Request на GitHub.

Рекомендации:
- Следуйте Dart style guide.
- Добавляйте тесты для новых функций.
- Проверяйте линтер: `flutter analyze`.

---

## 🐛 Известные проблемы и решения

### Ошибка SSL при сборке (Windows)
Если при сборке (`flutter build`) возникает ошибка `HandshakeException` при скачивании бинарных файлов Realm, выполните скрипт для ручной установки:

```powershell
powershell -ExecutionPolicy Bypass -File fix_realm.ps1
```

Этот скрипт скачает необходимые файлы в кэш пуба, минуя проверку SSL сертификата.

### Другие проблемы
- Приложение не запускается: попробуйте `flutter clean` → `flutter pub get`.
- Проблемы с Realm: очистите данные приложения (локальное хранилище) или пересоздайте базу.
- Ошибки сборки нативных модулей: убедитесь, что установлены необходимые инструменты (NDK/CMake/Xcode) и выполните `flutter pub get`.

---

## 🎯 Roadmap (планы)
- Облачная синхронизация через Realm Cloud
- Уведомления и напоминания
- Категории, теги, повторяющиеся задачи
- Статистика и аналитика
- Интеграция с календарём
- Экспорт/импорт данных, совместная работа, i18n

---

## 📬 Контакты и поддержка
- Сообщить об ошибке / предложении: https://github.com/Beksanoff/todo-list/issues
- Обсуждения: https://github.com/Beksanoff/todo-list/discussions

---
